name: Update .gitignore

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-gitignore:
    uses: kitsuyui/gh-actions-workflows/.github/workflows/gitignore-in.yml@866dfed65d3117b3825c5ca312f2b64cdae04285 # main
